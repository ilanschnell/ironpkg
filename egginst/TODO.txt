TODO:
-----

  * bug: dead links
  * -a, --activate
  * -d, --deactocate
    Move files and directories into backup directory:
    ... backup/numpy-1.2.1/numpy  (dir)
                          /bin/f2py
                          /lib/livg2c.so

  * -i, --install-dir
    For example -i /tmp would install for numpy:
        /tmp/bin/f2py
            /lib/libg2c.so
            /numpy  (dir)

  * -r, --remove
